>   - name: {{ name }}
>     description: {{ help.sanitize_for_print }}
>     usage: {{ usage_string.sanitize_for_print }}
>     options:
if flags.any?
  flags.each do |flag|
    >     - names:
    >       - {{ flag.name }}
    >       argument: ""
    >       description: {{ flag.help.gsub("\n", '. ').gsub(":", ' ') }}
  end
end