>   - name: {{ name }}
>     description: | 
>         {{ help.gsub("\n", "\n        ").gsub(":", ' ') }}
>     usage: {{ usage_string.sanitize_for_print }}
if flags.any?
>     options:
  flags.each do |flag|
    >     - names:
    >       - {{ flag.name }}
if flag.short
    >       - {{ flag.short }}
end
    >       argument: ""
    >       description: | 
    >         {{ flag.help.gsub("\n", "\n        ").gsub(":", ' ') }}
  end
else
>     options: []
end
>     subcommands:
>