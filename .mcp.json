{
  "mcpServers": {
    "mcp-playground": {
      "command": "npx",
      "args": [
        "--registry=https://registry.npmjs.org",
        "-y",
        "github:vdesabou/kafka-docker-playground-mcp-server"
      ],
      "env": {
        "PLAYGROUND_REPO_ROOT": "${PWD}"
      }
    }
  }
}
